import React from 'react';
// Components
import Campfire from 'components/Campfire';
// Styles
import './index.scss';

const Home = ({ history }) => (
  <>
    <Campfire history={history} />
  </>
);

export default Home;
